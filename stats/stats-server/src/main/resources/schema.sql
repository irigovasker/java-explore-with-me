CREATE TABLE IF NOT EXISTS hit
(
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY ,
    app VARCHAR(100) ,
    uri VARCHAR(100) ,
    ip VARCHAR(25) ,
    hit_at TIMESTAMP WITHOUT TIME ZONE
);